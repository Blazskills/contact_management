Logout user
---
tags:
  - Authentication
parameters:
  - in: header
    name: Authorization
    required: true

responses:
  200:
    description: Logout Successful(Ok)

  500:
    description: Server error
